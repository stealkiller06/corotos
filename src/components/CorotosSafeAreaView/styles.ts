import {StyleSheet} from 'react-native'
import { CorotosBackgroundColor } from '../../_core/colors'

export default StyleSheet.create({
    CorotosSafeAreaViewStyle:{
        flex:1,
        backgroundColor:CorotosBackgroundColor,
    },
    viewStyle:{
        padding:20
    }
})